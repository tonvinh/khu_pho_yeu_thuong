# 24 — QC toàn diện 4/9/2026 (pass 1 + đã sửa tại chỗ)

> Nguồn đối chiếu: `docs/lp/LandingpageFCM.fig` node **`7217:1990`** (khổ 1440×3780) —
> đọc bằng `python3 scripts/figma/dump.py`. Số đo web lấy bằng **DOM thật trong Chrome**
> (iframe `width:1457px` → `clientWidth` 1440), không đo bằng mắt.
> ⚠️ `.fig` này **cũ hơn file trên figma.com** (xem `CLAUDE.md` mục "Figma LIVE 4/9"):
> nút mỗi dòng tab 2/tab 3 trong `.fig` còn nhãn cũ. Mọi số đo KHÁC vẫn dùng được.

## Tổng kết

| Nhóm | Tìm được | Đã sửa | Còn treo |
|---|---|---|---|
| A · trang chủ (bố cục/nền) | 7 | 6 | 1 (chân trang — chờ Design) |
| B · popup | 1 | 0 | 1 (nhỏ) |
| C · trang phụ | 1 | 0 | 1 (cấu hình dev) |
| D · admin | 4 | **4** | 0 |
| E · nghiệp vụ/API | 0 | — | — |
| F · responsive/chất lượng | 3 | 2 | 1 (nhỏ) |

Trang chủ khổ 1440: **3853px → 3760px** sau khi sửa (Figma 3780; chênh còn lại do dữ liệu
dev chỉ có 5 góc phố mở thay vì 6 dòng như design + chân trang 5 dòng).

---

## A · Trang chủ

### A1 · Sàn gạch lệch 110px → hình rời trông “lơ lửng” ✅ ĐÃ SỬA
- Mức độ: **Cao** (đúng lỗi team khoanh đỏ trong ảnh chú thích)
- Đo được: sàn gạch `plaza.webp` ở **y=814.5 h=323.9**; Figma `123123 1` **y=704 h=594**.
- Hệ quả: gánh hàng rong (`01 1` y=739) và ông cháu quét sân (`02 2` y=708) nằm **trên**
  mép sàn → trông như treo giữa không trung. Bản thân 3 hình rời **đúng toạ độ .fig tuyệt
  đối** (đo ở cả 1440/1512/1600/1920/1280, tỉ lệ so với KV không đổi) — lỗi nằm ở SÀN.
- Nguyên nhân: `HomeShell.tsx` đặt sàn `absolute inset-x-0 bottom-0 h-[58%]` theo khung
  ngoài (chiều cao khung phụ thuộc ảnh KV), không theo hệ toạ độ 1440 của `.fig`.
- Đã sửa: chuyển sàn + cung nét đứt vào **cùng khung 1440** với KV, quy % theo chiều cao KV
  (`top-[22.2%] h-[106.36%]`). Đo lại: **y=703.8 h=593.2** ✔

### A2 · Cung nét đứt bị cắt mất 86px ✅ ĐÃ SỬA
- Đo được: `hero-arc` **w=1440.6**; Figma `Frame 151 1` **x=-54 w=1526**.
- Nguyên nhân: `img { max-width: 100% }` của Tailwind preflight chặn `w-[105.97%]`.
- Đã sửa: thêm `max-w-none` → đo lại **x=-53.9 w=1524** ✔

### A3 · Skyline thấp hơn design 264px ✅ ĐÃ SỬA
- Đo được: **h=602.5**; Figma `Artboard 2 1` **1440×867** (ảnh ở chế độ FILL nên bị phóng
  và cắt hai bên — chính là cách node `abstract-pui-89…` x=-316 w=2072 mô tả).
- Đã sửa: `aspect-[1440/867] object-cover` → **h=865.9** ✔

### A4 · Mô tả khối “Đóng góp” chỉ 1 dòng → cả khối dưới lệch 24px ✅ ĐÃ SỬA
- Đo được: mô tả **w=1272 h=24**; Figma **x=296 w=848 h=48** (2 dòng).
- Hệ quả: tab ở y=1465.5 (Figma 1490), card y=1547 (Figma 1570).
- Đã sửa: `SectionHead` bó `max-w-[848px]` → mô tả **295.1/1415.7 848×48**, tab **1488.7**,
  card **1570.7** ✔

### A5 · Khối nội dung rộng 1272 thay vì 1276 (lệch lề 2.3px) ✅ ĐÃ SỬA
- Đo được: `#goc-xom` nội dung **x=84.3 w=1272**; Figma **x=82 w=1276**.
- Nguyên nhân: `max-w-[1312px] sm:px-5` (1312−40). Đã đổi `sm:px-[18px]` → **x=81.1 w=1276** ✔
  (áp cho cả `IssueBoard` và `SignGallery`.)

### A6 · Nhịp dọc giữa các khối ✅ ĐÃ SỬA
| Khoảng | Figma | Web (trước) | Web (sau) |
|---|---|---|---|
| đáy card → tiêu đề khối biển | 66 | 80 | **66** ✔ |
| tiêu đề biển → lưới biển | 40 | 40 | 40 ✔ |
| lưới biển → panel ưu đãi | 80 | 80 | 80 ✔ |
| đáy nút form → khối chữ chân trang | 64 | **160** | **64** ✔ |
| lề trong đáy card danh sách | 24 | 32 | **24** ✔ |

### A7 · Chân trang 5 dòng thay vì 3 — ✅ ĐÓNG (chốt 4/9: GIỮ NGUYÊN)
- Đo được: khối chữ **697×134**; Figma `Frame 221` **697×84**.
- Dòng `footer_support` (“Đã là khách hàng của FPT… gọi 1900 6600…”) chiếm 2 dòng.
- **Chốt**: giữ nguyên đủ 4 dòng chân trang, gồm cả câu “Đã là khách hàng của FPT…”.
  Trang cao hơn `.fig` ~50px ở đoạn này là **chấp nhận có chủ ý**, không sửa.

---

## B · Popup (đo trên DOM thật, khổ 1440)

Đã mở và bấm thử: **Đề xuất góc phố · Gửi câu nhắc · Chọn góc phố · Định danh ·
Thông tin khu phố · Cây bút khu phố**. Kết quả:

- Khung 700, viền cam 2px, tiêu đề 25px Regular, sọc trong khung — ✔
- **Khoá cuộn nền**: mở popup → `body { position: fixed; top: -600px }`, `scrollTo(0,2000)`
  không làm nền trôi; Esc đóng → scroll trả về **đúng 600** ✔ (C2/C3 của QC 2/9 vẫn ổn)
- 4 chip 4N trong “Gửi câu nhắc” là `<div>` kèm chú thích, **không phải `<button>`** ✔ (Q4)
- “Gửi câu nhắc” hiện sẵn tên chủ đề + phường, **không còn loé “Đang tải…”** ✔ (C4)
- Ô viết câu chặn ở 120 ký tự + đếm `n/120` ✔

### B1 · Popup “Thông tin khu phố” trống ~1.5–2s trước khi có dữ liệu — 🟡 Thấp
Mở ra chỉ có tiêu đề, nội dung (tên khu, danh sách câu) đổ về sau khi fetch xong.
Cách sửa: truyền sẵn tên + phường từ `data.map` như `SuggestModal` đã làm (C4 cũ).

---

## C · Trang phụ

Đã mở: `/khu-pho/hem-cho-xom-moi` · `/dai-su/pzyp9tnzel` · `/bien/<id câu đã treo>` ·
`/chinh-sach-du-lieu` · `/khu-pho/khong-co-that` (→ **404 đúng**).
Không tràn ngang, không ảnh hỏng, không `font-semibold/extrabold`, OG title/description/image đủ.

### C1 · `og:url` trỏ `localhost:3000` trong khi server chạy 3001 — 🟡 Thấp (cấu hình)
`SITE_ORIGIN` trong `.env` là `http://localhost:3000`. Không phải lỗi code; nhớ đặt đúng
domain khi lên production, nếu không link chia sẻ ra Facebook/Zalo sẽ sai.

---

## D · Admin (đã QC sau khi anh/chị đăng nhập)

Đã mở và bấm thử **7 màn**: Dashboard · Khu phố (2 tab) · Lời nhắc (2 tab) · Theo dõi
thương · Leads · Nội dung · Chống gian lận. Không màn nào lỗi console, không request 4xx/5xx
ngoài dự kiến.

**Chạy đúng**
- Chặn truy cập: `/admin`, `/admin/khu-pho` → **307 → /admin/login**; API không phiên → **401**.
- `useUrlState` giữ trạng thái trên query string khi đổi tab/lọc/tìm ✔
- Khu phố: 6 bộ lọc kèm số đếm, tìm kiếm, 20 dòng/trang; tab **Đề xuất góc phố** đủ 8 cột +
  5 chip trạng thái + thao tác Duyệt / Từ chối…
- Lời nhắc: 6 chip trạng thái, thao tác Duyệt / Từ chối / ✏️ Sửa; tab **Chọn câu & vòng đời
  biển** đủ 3 chặng (đang bình chọn → đã chọn → đang sản xuất, kèm ô ngày treo riêng từng dòng).
- Leads: **SĐT che `090***410`** trong danh sách ✔, cột Tỉnh thành + Địa chỉ có ✔,
  CSV `format=csv` trả 9 cột **có `tinh_thanh`, `dia_chi`** ✔
- **Bấm-để-hiện SĐT** trả số đầy đủ và ghi `audit_logs.lead_phone_reveal`; export CSV ghi
  `leads_export_csv` ✔ (kiểm trực tiếp trong DB)
- Nội dung: PATCH `hero_title` → **trang chủ đổi ngay** → khôi phục lại rỗng ✔;
  PATCH **thiếu `x-csrf-token` → 403** ✔
- Import: modal mô tả 3 cột, có link tải mẫu .xlsx, nhận .xlsx/.csv, all-or-nothing ✔
- Nhãn 6 dịch vụ mới hiện đúng cho **lead cũ** (Q3): “Truyền hình FPT Play”,
  “Internet + Camera”… ✔

**Lỗi tìm được**

### D1 · `/api/admin/leads/<id không phải uuid>` trả 500 ✅ ĐÃ SỬA
- `GET /api/admin/leads/abc` → **500**, log server ném `invalid input syntax for type uuid`
  (lỗi Postgres lọt ra ngoài). Các route `[id]` khác trả 404/405 đúng.
- Đã sửa: chặn sớm bằng regex uuid trong `src/app/api/admin/leads/[id]/route.ts` (cả GET lẫn
  PATCH) → **404**. Đo lại: id sai 404, id đúng-nhưng-không-có 404 ✔

### D2 · `/admin/loi-nhac` tràn ngang ở khổ 375 ✅ ĐÃ SỬA
- `scrollWidth 399 > clientWidth 377` (+22px). Thủ phạm: `<input>`/`<select>` trong lưới lọc
  có bề rộng nội tại ~363px, ô lưới `min-width: auto` nên không co lại.
- Đã sửa: thêm `w-full min-w-0` cho `SearchBox` (dùng chung) và 3 `<select>` cùng kiểu.
  Đo lại 375: `/admin/loi-nhac`, `/admin/khu-pho`, `/admin/leads` đều **không tràn** ✔

### D3 · Bốn khoá `campaign_*` là trường chết trong `/admin/noi-dung` ✅ ĐÃ GỠ (chốt 4/9)
`campaign_title`, `campaign_hint`, `campaign_youtube_ids`, `campaign_kv_url` vẫn sửa được
trong admin nhưng **không render ở đâu** trên site (khối TVC `CampaignMedia.tsx` đã xoá từ
2/9). Đúng loại lỗi đã dọn hôm 3/9 với 4 khoá `sign_promo_*`.
- Đã gỡ: 4 khoá khỏi `SITE_CONTENT_DEFAULTS` + `SiteContentData`, khối “Video TVC + KV
  chiến dịch” khỏi `/admin/noi-dung`, route `/api/admin/site-content/kv` (xoá cả thư mục),
  `parseYoutubeIds` / `LEGACY_VIDEO_KEY` / `SITE_KV_KEY` và nút upload không còn ai dùng.
- Hàng cũ `campaign_*` trong bảng `site_content` chỉ bị lơ đi — **không cần migration**
  (đúng cách đã làm với `sign_promo_*` hôm 3/9). Chuỗi copy vẫn nằm ở `copy.ts` nếu cần bật lại.
- Khoá đo lại: API admin trả **13 khoá**, không còn khoá nào bắt đầu bằng `campaign`,
  không còn `kv_url`; trang chủ vẫn render bình thường. Test khoá: `tests/site-content.test.ts`.

### D4 · Tiêu đề tab riêng cho từng trang ✅ ĐÃ SỬA (chốt 4/9)
Trước: `/admin/login` và mọi màn admin dùng chung tiêu đề trang chủ công khai.
Nay layout `(panel)` khai `template: "%s — Admin Khu Phố"`, mỗi route có `layout.tsx`
riêng khai tên ngắn (trang là client component nên không tự khai metadata được):

| Route | Tiêu đề |
|---|---|
| `/admin` | Dashboard — Admin Khu Phố |
| `/admin/khu-pho` | Khu phố — Admin Khu Phố |
| `/admin/loi-nhac` | Lời nhắc — Admin Khu Phố |
| `/admin/voting` | Theo dõi thương — Admin Khu Phố |
| `/admin/leads` | Leads — Admin Khu Phố |
| `/admin/noi-dung` | Nội dung — Admin Khu Phố |
| `/admin/gian-lan` | Chống gian lận — Admin Khu Phố |
| `/admin/login` | Đăng nhập — Admin Khu Phố (kèm `robots: noindex`) |

Trang công khai giữ nguyên tiêu đề riêng sẵn có (`/khu-pho/[slug]`, `/dai-su/[slug]`,
`/bien/[id]`, `/chinh-sach-du-lieu`). Test khoá: 2 ca e2e mới.

### Đã DUYỆT THỬ toàn bộ luồng ghi dữ liệu (4/9, trên DB dev)

| Luồng | Kết quả kiểm bằng psql |
|---|---|
| Duyệt đề xuất | `pending_review → waiting`, khu phố tự nhập được **bỏ ẩn**, `+2đ`, notification `issue_approved` ✔ |
| Từ chối đề xuất | `rejected` + `review_note`, notification `issue_rejected` ✔ |
| Duyệt câu (bắt buộc tick đủ 4N) | `submitted → approved`, lưu `review_4n`, `+5đ`, issue tự sang `voting` ✔ |
| Chọn câu lên biển | `approved → selected` (không phải câu cao phiếu nhất thì bắt nhập lý do) ✔ |
| Đưa sản xuất → treo biển | `selected → produced → installed` + `installed_date`, issue `signed`, `+30đ`, notification `sign_installed` ✔ |
| Sửa số thương | 45 → 48 bằng 3 phiếu `source='admin'`, audit `votes_adjust {from,to}` ✔ |
| Đổi trạng thái lead | `new → contacted → converted` ✔ |
| Hiện SĐT / export CSV | audit `lead_phone_reveal`, `leads_export_csv` ✔ |
| Sửa nội dung trang chủ | PATCH đổi ngay trên trang chủ; thiếu CSRF → 403 ✔ |

**Ba lỗ hổng tìm thấy khi duyệt thử — đã sửa:**

### D5 · Không có nhật ký cho mọi thao tác duyệt ✅ ĐÃ SỬA
`audit_logs` chỉ ghi leads + votes + site-content. Duyệt/từ chối đề xuất, duyệt/từ chối/chọn
câu, đưa sản xuất, treo biển **không để lại dấu vết**, mà bảng `issues`/`suggestions` cũng
không có cột người duyệt → không truy được ai làm gì.
Nay ghi `issue_approve` / `issue_reject` / `suggestion_<hành động>` kèm `detail`
(vị trí, nội dung câu, trạng thái cũ, lý do).

### D6 · Câu nhắc gửi kèm đề xuất bị từ chối kẹt vĩnh viễn ✅ ĐÃ SỬA
Hàng duyệt câu lọc bỏ câu của issue chưa duyệt, nên khi đề xuất bị từ chối thì câu gửi kèm
ở lại `submitted` và **không màn nào nhìn thấy nữa**. Nay từ chối đề xuất kéo theo câu kèm
(`review_note` ghi “Đề xuất góc phố bị từ chối: …”). Đã dọn câu mồ côi còn sót: hiện **0**.

### D7 · Ô nhập lý do từ chối 11px trong cột thao tác ✅ ĐÃ SỬA
Gõ lý do vào ô rộng 92px, chữ 11px — không đọc nổi câu mình vừa viết. Nay là **popup** có
ngữ cảnh (chủ đề · vị trí · khu phố · người đề xuất · mô tả), textarea 3 dòng kèm ví dụ lý
do, và câu nhắc “câu kèm cũng bị từ chối theo”.

### D8 · Popup admin thiếu a11y + `IssuesPanel` loé “Hàng chờ trống 🎉” ✅ ĐÃ SỬA
Popup admin gom về `components/admin/modal.tsx`: thêm `role="dialog"`, `aria-modal`,
`aria-label`, đóng bằng **Esc**. `IssuesPanel` thêm cờ `loaded` → khung hình đầu hiện
“Đang tải…” thay vì báo hàng chờ trống trong khi thật ra có 6 đề xuất.

**Chưa bấm thử**: import file thật (đã có e2e phủ), thêm/sửa khu phố + upload ảnh.

## E · Nghiệp vụ / API — không thấy lỗi

- `GET /api/v1/notes` trả đủ trường dòng tab 2, **không lộ SĐT/email** ✔ (2 ca e2e mới)
- Bình chọn: lần 1 ăn (28→29), lần 2 **409 `Bạn đã bình chọn câu này rồi`**, số phiếu
  không đổi, điểm tác giả không bị thu hồi ✔ (Q6)
- Cấm tự thương ✔ · Trần “3 SĐT mới/thiết bị/giờ” ✔ (e2e)
- Ô tra cứu đủ **3 trạng thái** đúng design: nhiều kết quả (pill xanh “Đạt chuẩn 4N”),
  đúng 1 kết quả (card kem + câu mời + “Xem khu phố”), rỗng (“Chưa tìm thấy khu phố này”
  + “+ Đề xuất góc phố mới”) ✔
- Console: **0 lỗi** (chỉ log dev của React/Fast Refresh). Network: **24 request, toàn
  200/304**, không 4xx/5xx.

---

## F · Responsive & chất lượng

- `scrollWidth == clientWidth` ở **375 / 414 / 768 / 1024 / 1280 / 1440** ✔ không tràn ngang.
- **F1 ✅ ĐÃ SỬA** — vùng chạm nhỏ hơn chuẩn 44px ở mobile: avatar `35×35` → **44×44**
  (desktop giữ 35 theo .fig); mũi tên slider `36` → **44** (desktop giữ 40).
- **F2 🟡 Thấp** — ô checkbox “Tôi đồng ý…” cao 18px (đúng .fig `18×18 r=6`) nhưng vùng
  chạm dưới 44. Nhãn bấm được nên chấp nhận; muốn chuẩn thì bọc `<label>` cao 44.

---

## G · Câu hỏi chặn — trạng thái sau khi chốt 4/9

| # | Nội dung | Kết luận |
|---|---|---|
| 1 | Chân trang bỏ bớt dòng? | ✅ **Giữ nguyên** cả câu “Đã là khách hàng của FPT…”. Không sửa. |
| 2 | Dòng đầu tab 2 màu cam là gì? | ✅ **Không tái hiện được** — mở lại frame `7651:1537`, để chuột ra xa và rê lên từng dòng đều thấy **cả 5 dòng màu đậm**. Ảnh chụp đầu tiên nhiều khả năng dính khung render dở lúc prototype đang tải. Web đang render tất cả màu đậm ⇒ **khớp, không phải sửa**. |
| 3 | Dropdown 1 kết quả cho khu **đã đạt chuẩn 4N** | 🔴 **CÒN TREO** — xem giải thích + ảnh ở dưới. |
| 4 | Lấy `.fig` nào làm chuẩn? | ✅ **Căn cứ `.fig` hiện tại trong repo** cho mọi số đo (toạ độ, màu, khoảng cách). Riêng hai nhãn nút tab 2/tab 3 giữ theo quyết định 4/9 (`Bình chọn` xanh ở tab 2, `Xem lời nhắc` cam ở tab 3) vì đó là yêu cầu đã chốt bằng lời. |
| 5 | Popup “Chọn góc phố” có frame riêng? | Chưa có trong `.fig` → dựng theo khung Modal chung, đổi khi Design gửi frame. |

### Giải thích câu 3 (đang treo)

Gõ đúng tên **một** khu phố vào ô tra cứu → dropdown hiện card kết quả + **một dòng mời cố
định**: *“Khu phố mình chưa có nhiều lời nhắc, bạn viết câu đầu tiên nhé?”* kèm nút
`Xem khu phố`. Design chỉ vẽ **duy nhất** câu này.

Vấn đề: câu đó đúng với khu phố mới, nhưng **sai với khu đã Đạt chuẩn 4N** — khu đó đã có
đủ lời nhắc và đã treo biển, vẫn bị mời “viết câu đầu tiên”. Ví dụ thật trên web: khu
“Hẻm 51 Cao Thắng” có pill **Đạt chuẩn 4N** ngay bên phải mà dòng dưới vẫn nói *chưa có
nhiều lời nhắc*.

**Cần Design/BA chốt**: viết câu thứ hai cho khu đã đạt chuẩn (ví dụ “Khu phố này đã đạt
chuẩn 4N — xem những lời nhắc đã lên biển nhé!”), hay chấp nhận dùng chung một câu?
